@model IEnumerable<Produto>
@{
    ViewBag.Title = "Todos os produtos";
    Layout = "_AdminLayout";
}
<table class="table table-striped table-bordered table-condensed">
    <tr>
        <th class="text-right">ID</th>
        <th>Nome</th>
        <th class="text-right">Preço</th>
        <th class="text-center">Ações</th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td class="text-right">@item?.ProdutoID</td>
            <td>@item?.NomePD</td>
            <td class="text-right">@item?.PrecoPD.ToString("c")</td>
            <td class="text-center">
                <form asp-action="Delete" method="post">
                    <a asp-action="Edit" class="btn btn-sm btn btn-warning" asp-route-batata="@item.ProdutoID">Edit</a>
                    <input type="hidden" name="ProdutoID" value="@item.ProdutoID" />
                    <button type="submit" class="btn btn-danger btn-sm" >Delete</button>
                </form>
            </td>
        </tr>
    }
</table>
<div class="text-center">
    <a asp-action="Create" class="btn btn-primary">Adicionar produto</a>
</div>