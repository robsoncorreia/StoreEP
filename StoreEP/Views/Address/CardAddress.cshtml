@model Address
<div class="card">
    <div class="card-block">
        <h4 class="card-title">@Model.City</h4>
        <h6 class="card-subtitle mb-2 text-muted">@Model.Line1<span> @Model.Line3</span></h6>
        <p class="card-text">@Model.Line2</p>
        <a href="#" class="card-link">User este</a>
    </div>
</div>
