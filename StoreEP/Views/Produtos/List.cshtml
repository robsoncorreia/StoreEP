@using StoreEP.Models.ViewModels
@model ProductsListViewModel

@{
    ViewData["Title"] = "Index";
}
@foreach (var p in Model.Produtos)
{
    @Html.Partial("CardProduto", p);
}
<div page-model="@Model.PagingInfo" page-action="List" page-classes-enabled="true" page-class="btn" page-class-normal="btn-default" page-class-selected="btn-primary" class="btn-group pull-right"></div>
