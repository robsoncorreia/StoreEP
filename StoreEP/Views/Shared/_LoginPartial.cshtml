@using Microsoft.AspNetCore.Identity
@using StoreEP.Models
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

@if (SignInManager.IsSignedIn(User))
{
    <form asp-area="" asp-controller="Account" asp-action="Logout" method="post" id="logoutForm" style="float: right !important;">
        <ul class="nav navbar-nav">
            <li>
                <a asp-area="" asp-controller="Manage" asp-action="Index" title="Manage">
                    <span class="glyphicon glyphicon-user"> Olá @UserManager.GetUserName(User)!</span>
                </a>
            </li>
            <li>
                <button type="submit" class="btn btn-link navbar-btn navbar-link">
                    <span class="glyphicon glyphicon-log-out"> Sair</span>
                </button>
            </li>
        </ul>
    </form>
}
else
{
    <ul class="nav navbar-nav" style="padding: 0 10px 0; float: right !important;">
        <li>
            <a asp-area="" asp-controller="Account" asp-action="Register" asp-route-returnUrl="@ViewContext.HttpContext.Request.PathAndQuery()">
                <span class="glyphicon glyphicon-user"> Registrar</span>
            </a>
        </li>
        <li>
            <a asp-area="" asp-controller="Account" asp-action="Login">
                <span class="glyphicon glyphicon-log-in">
                    Entrar
                </span>
            </a>
        </li>
    </ul>
}
